# audio(LINUX ONLY)

## Fast Fourier Transform(FFT)

exec.file is created by command:  
	
	make compile_fft

exec.file is executed by command:  

	make start_fft

## Split by vad

exec.file is created by command:  
	
	make compile_vad

exec.file is executed by command:  

	make start_vad
